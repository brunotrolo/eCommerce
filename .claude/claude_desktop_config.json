{
  "$schema": "https://schemas.anthropic.com/claude-desktop-config-v1.json",
  "mcpServers": {
    "marketplace-connect": {
      "command": "node",
      "args": [
        "-e",
        "const McpClient = require('./dist/mcp/mcp-client').McpClient; const client = new McpClient(process.env.MARKETPLACE_API_KEY); console.log('MCP Server iniciado'); process.stdin.on('data', d => {})"
      ],
      "env": {
        "MARKETPLACE_API_KEY": "mc_live_YOUR_KEY_HERE"
      }
    }
  }
}
